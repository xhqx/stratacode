name: triage

on:
  issues:
    types: [opened]

jobs:
  triage:
    if: github.repository == 'Strata-Org/stratacode'
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Setup Strata
        uses: ./.github/actions/setup-strata

      - name: Triage issue
        env:
          STRATA_API_KEY: ${{ secrets.STRATA_API_KEY }}
          STRATA_ORG_ID: ${{ secrets.STRATA_ORG_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          strata run --agent triage "The following issue was just opened, triage it:

          Title: $ISSUE_TITLE

          $ISSUE_BODY"
